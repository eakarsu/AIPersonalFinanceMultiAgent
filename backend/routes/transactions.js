const express=require('express'),pool=require('../models/db'),auth=require('../middleware/auth'),r=express.Router();
r.get('/',auth,async(q,s)=>{try{let query='SELECT t.*,a.name as account_name FROM transactions t LEFT JOIN accounts a ON t.account_id=a.id';const p=[];const c=[];if(q.query.category){p.push(q.query.category);c.push(`t.category=$${p.length}`)}if(q.query.search){p.push(`%${q.query.search}%`);c.push(`t.merchant ILIKE $${p.length}`)}if(c.length)query+=' WHERE '+c.join(' AND ');query+=' ORDER BY t.date DESC';s.json((await pool.query(query,p)).rows)}catch(e){s.status(500).json({error:e.message})}});
r.post('/',auth,async(q,s)=>{try{const{account_id,merchant,category,amount,transaction_type,date,description}=q.body;const r=await pool.query('INSERT INTO transactions(account_id,merchant,category,amount,transaction_type,date,description) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING *',[account_id,merchant,category,amount,transaction_type||'debit',date,description]);s.status(201).json(r.rows[0])}catch(e){s.status(500).json({error:e.message})}});
r.put('/:id',auth,async(q,s)=>{try{const{merchant,category,amount,description}=q.body;const r=await pool.query('UPDATE transactions SET merchant=$1,category=$2,amount=$3,description=$4 WHERE id=$5 RETURNING *',[merchant,category,amount,description,q.params.id]);s.json(r.rows[0])}catch(e){s.status(500).json({error:e.message})}});
r.delete('/:id',auth,async(q,s)=>{try{await pool.query('DELETE FROM transactions WHERE id=$1',[q.params.id]);s.json({message:'Deleted'})}catch(e){s.status(500).json({error:e.message})}});
module.exports=r;
